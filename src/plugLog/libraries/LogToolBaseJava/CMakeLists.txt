FILE( GLOB LOGSERVICE_LOGTOOLBASEJAVA_SOURCES_GLOB "*.java" )

FOREACH(file ${LOGSERVICE_LOGTOOLBASEJAVA_SOURCES_GLOB} )
  INSTALL( FILES ${file} DESTINATION lib/LogServiceLogToolBaseJava )
ENDFOREACH(file ${LOGSERVICE_LOGTOOLBASEJAVA_SOURCES_GLOB} )

